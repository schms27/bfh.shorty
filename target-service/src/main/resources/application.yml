# Spring properties
spring:
  application:
    name: target-service
  h2:
    console:
      enabled: true
      path: /h2

# Discovery Server Access
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/

# HTTP Server
server:
  port: 4444  # HTTP (Tomcat) port